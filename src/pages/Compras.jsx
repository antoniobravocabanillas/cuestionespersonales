import Header from '../components/layout/Header';import MetricCard from '../components/layout/MetricCard';import { totalComprasEstimadas } from '../modules/compras/comprasUtils';import { formatMoney } from '../modules/finanzas/finanzasUtils';
export default function Compras({ compras }){return <><Header title='Lista de compras inteligente' subtitle='Planifica compras y conecta comida, presupuesto y gasto real.'/><div className='grid grid-3'><MetricCard label='Estimado total' value={totalComprasEstimadas(compras)}/><MetricCard label='Items pendientes' value={compras.filter(i=>i.estado==='Pendiente').length} money={false}/><MetricCard label='Categorías' value={[...new Set(compras.map(i=>i.categoria))].length} money={false}/></div><div className='card section'><table className='table'><thead><tr><th>Item</th><th>Categoría</th><th>Cantidad</th><th>Estimado</th><th>Estado</th></tr></thead><tbody>{compras.map(i=><tr key={i.id}><td>{i.item}</td><td>{i.categoria}</td><td>{i.cantidad}</td><td>{formatMoney(i.estimado)}</td><td><span className='badge'>{i.estado}</span></td></tr>)}</tbody></table></div></>}
